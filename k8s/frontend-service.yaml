apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: irene-2tier   # replace with your namespace
spec:
  type: ClusterIP
  selector:
    app: frontend         # matches the label on your frontend Deployment
  ports:
    - port: 80            # service port
      targetPort: 80      # container port